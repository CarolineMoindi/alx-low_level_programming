#include <stdio.h>
/**
 * Description: main - Prints out a string to stdout.
 * return: 0 if success.
 */
int main(void)
{
	printf("with proper grammar, but the otucome is a piece of art,\n");
	return (0);
}
